# CarbonMC handoff — 2026-09-24

The interrupted encrypted-login baseline is implemented and validated at `fd8c46e0fb3af76f0ae70b0588224a45ce0dc65c`: RSA challenge exchange, continuous AES-CFB8, bounded Mojang session verification, canonical UUID/name admission, cancellation/deadline/capacity controls, and encrypted compression/configuration tests. Existing work and upstream README changes were preserved; no subsequent gameplay milestone was started. Root AGENTS.md was removed upstream; its saved local guidance and REFERENCE_POLICY.md were followed. No Pumpkin implementation was needed or copied.

Validation: local Windows gnullvm formatting, locked workspace check/build, strict Clippy, 231 Rust tests (two existing subprocess helpers ignored), ten Python regressions, ten isolated CLI/recovery invocations, and online-mode startup/status/shutdown passed. [Standard CI](https://github.com/CarbonMC-Server/CarbonMC/actions/runs/35860709118) and [native release validation](https://github.com/CarbonMC-Server/CarbonMC/actions/runs/35860709097) passed for the baseline. Windows MSVC/Linux GNU each passed independent exact archive comparison and fresh-package smoke/save acceptance; eight package process checks include online crypto initialization with build tools removed from PATH. OpenSSL build-path nondeterminism and cc-rs quoting failures were fixed without weakening checks or reusing native outputs.

GitHub: `origin` is CarbonMC-Server/CarbonMC; changes were committed and normally pushed to `main`, which had no PR requirement. Native evidence: eight checks and 315 file hashes on Windows, eight checks and 313 hashes on Linux. Build tools, logs and helpers remain ignored under work/; do not commit them. Local gnullvm compilation uses the existing MSYS Perl/Make via OPENSSL_SRC_PERL and PATH.

Next: arrange licensed-client online login, invalid/expired-session rejection, reconnect and two-client encrypted gameplay in disposable worlds. Synthetic session tests do not establish real Mojang-account acceptance. Default skins and server-authored system chat remain; signed chat/property verification, broader security, power-loss acceptance and distribution provenance remain open. Do not advertise production readiness.
